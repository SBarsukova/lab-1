#include <stdio.h>
#include <limits.h>
#include <cstdlib>
#include <time.h>
#include <iostream>

using namespace std;

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) 
	{
	int i, n, m, c1, c2, j;
	int matrix[n][m];
	long ltime = time (NULL);
    int stime = (unsigned int) ltime/2;
	srand(stime);
	cout << "Enter matrix size\n";
	cin >> n;
	cin >> m;
	cout << "enter first column num\n";
	cin >> c1;
	cout << "enter second column num\n";
	cin >> c2;
	for (i=0; i<n; i++)
	{	for (j=0; j<m; j++)
		{
			matrix[i][j] = rand();
			cout << matrix[i][j] << "\t";
		}
		cout << "\n";
	}
/*	for (i=0; i<n; i++)
	{	for (j=0; j<m; j++)
		{
			if ()
	
*/	}
