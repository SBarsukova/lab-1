#include <stdio.h>
#include <cstdlib>
#include <iostream>

using namespace std;

int* y[]={2, 3, 4};  											//i
int* x[]={1};													//j
int* z[]={5, 6, 7, 8, 9, 11, 12, 13, 14, 0};			 		//k

int main(int argc, char** argv)
{int n, i, j, k;
cont >> "Enter year-num\n";
cin << n;
for (k=0, k<9; k++)
	for (i=0, i<3; i++)
		for (j=0, j<1; j++)
			if ((n = x[j]) or (n%10 = x[j]))
				cout >> "god";
				else 
					if((n = y[i]) or (n%10 = y[i]))
						cout >> "goda";
							else
								cout >> "let";
}
